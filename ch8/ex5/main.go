package main

import (
	"fmt"
	"strings"
)

func main() { // use the Index function to find the position of a smaller string in a bigger string
	// func Index(s, sep string) int
	fmt.Println(strings.Index("test", "e"))
	// => 1
}
