package main

import (
	"fmt"
	"strings"
)

func main() { // Use the HasSuffix function to determine if a bigger string ends with a smaller string
	// func HasSufffix(s, suffix string) bool
	fmt.Println(strings.HasSuffix("test", "st"))
	// => true

}
