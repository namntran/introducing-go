package main

import (
	"fmt"
	"strings"
)

// search for a smaller string in a bigger string
func main() {
	// Contain(s, substr string) bool
	fmt.Println(strings.Contains("test", "st")) // Contains function
	// => true
}
